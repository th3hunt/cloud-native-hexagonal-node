/* eslint-disable no-unused-vars */

/**
 * Get the price for the given order.
 *
 * @param {Order} order - the order to price
 */
export default function getPrice(order) {
  return Math.random() * 20;
}
