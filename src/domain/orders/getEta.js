/* eslint-disable no-unused-vars */

/**
 * Get the ETA in minutes for an order to be delivered
 *
 * @param {Order} order - the order
 */
export default function getEta(order) {
  return Math.random() * 60;
}
